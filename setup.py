#   encoding: utf8
#   filename: setup.py
#
#   NOTE This file is used only for editable installations with
#   `pip install -e` command.

if __name__ == '__main__':
    from setuptools import setup
    setup()
